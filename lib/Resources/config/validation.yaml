DawBed\PHPToken\Token:
      properties:
         expired:
           - DawBed\ConfirmationBundle\Validator\Constraints\Token\Expired: { groups: [accept] }
         consume:
           - DawBed\ConfirmationBundle\Validator\Constraints\Token\Consume: { groups: [accept] }
DawBed\PHPToken\Model\AcceptModel:
      constrainsts:
         - Symfony\Component\Validator\Constraints\Vadlid:
             fielsds: [entity]
      properties:
         entity:
           - NotBlank: ~
           - Valid: ~

