DawBed\ConfirmationBundle\Entity\AbstractToken:
      properties:
         expired:
           - DawBed\ConfirmationBundle\Validator\Constraints\Token\Expired: { groups: [accept] }
         consume:
           - DawBed\ConfirmationBundle\Validator\Constraints\Token\Consume: { groups: [accept] }

DawBed\ConfirmationBundle\Model\WriteModel:
  properties:
    entity:
      - NotBlank: { groups: [accept] }
